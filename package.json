{
  "name": "majin-marp-slide",
  "version": "1.0.0",
  "description": "MCP server that returns a high-quality Marp slide generation prompt",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "majin-marp-slide": "./build/index.js"
  },
  "files": ["build"],
  "scripts": {
    "build": "tsc && chmod 755 build/index.js",
    "prepare": "npm run build",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "keywords": ["mcp", "model-context-protocol", "marp", "slides"],
  "author": "",
  "license": "MIT",
  "engines": { "node": ">=18" },
  "publishConfig": { "access": "public" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.3",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.5.4"
  }
}
